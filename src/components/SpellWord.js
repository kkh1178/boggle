import React from "react";

const SpellWord = ({ dice }) => {
    // get the index of the selected letter
    // if the next letter doesn't equal to +-1, +-4, +-5 or +-6, then you can't use that letter
    console.log(dice);
    return (
        <div>
            <p>word</p>
        </div>
    );
};

export default SpellWord;
